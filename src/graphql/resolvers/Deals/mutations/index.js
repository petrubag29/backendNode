const submitDeal = require('./submitDeal');
const updateDeal = require('./updateDeal');
const acceptDeal = require('./acceptDeal');
const deleteDeal = require('./deleteDeal');
const getDealFileUploadsSignedURLS = require('./getDealFileUploadsSignedURLS');

module.exports = {
  submitDeal,
  updateDeal,
  acceptDeal,
  deleteDeal,
  getDealFileUploadsSignedURLS,
};
